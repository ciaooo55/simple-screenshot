name: Build and release Windows app

on:
  push:
    branches: [main]
    tags: ["v*", "r*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: python -m pip install --upgrade pip -r requirements.txt
      - name: Run tests
        run: python -m pytest -q

  release:
    name: Package and publish
    needs: test
    if: startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/r')
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: python -m pip install --upgrade pip -r requirements.txt
      - name: Build portable executable
        run: .\build.ps1 -Clean
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        shell: pwsh
        run: |
          if (gh release view $env:TAG 2>$null) {
            gh release upload $env:TAG dist\SimpleScreenshot.exe --clobber
          } else {
            gh release create $env:TAG dist\SimpleScreenshot.exe `
              --title "SimpleScreenshot $env:TAG" --generate-notes
          }
