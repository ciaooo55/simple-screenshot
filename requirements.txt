PySide6>=6.8,<7
pyinstaller>=6.0,<7
pytest>=8,<10
# 高精度识别引擎(内置 PaddleOCR 模型,微信同类技术)。默认使用
# CPU 版 onnxruntime，避开部分显卡驱动的 DirectML 原生崩溃。
# rapidocr_onnxruntime 由 CI 单独 --no-deps 安装，避免依赖解析改写版本:
#   python -m pip install --no-deps "rapidocr_onnxruntime>=1.4,<2"
# 这里只声明它的其余依赖,CI 已按此两步安装。
onnxruntime>=1.20,<2; sys_platform == "win32"
numpy>=1.24,<3; sys_platform == "win32"
opencv-python>=4.8; sys_platform == "win32"
Pillow>=10; sys_platform == "win32"
pyclipper>=1.2; sys_platform == "win32"
PyYAML>=6; sys_platform == "win32"
Shapely>=2,!=2.0.4; sys_platform == "win32"
six>=1.15; sys_platform == "win32"
tqdm>=4; sys_platform == "win32"

# 系统 OCR 兜底:pywinrt 按命名空间分包,只装用到的投影。
# 平台标记让 Linux/macOS 开发环境跳过这些 Windows 专属包。
winrt-runtime>=3.2,<4; sys_platform == "win32"
winrt-Windows.Media.Ocr>=3.2,<4; sys_platform == "win32"
winrt-Windows.Graphics.Imaging>=3.2,<4; sys_platform == "win32"
winrt-Windows.Security.Cryptography>=3.2,<4; sys_platform == "win32"
winrt-Windows.Storage.Streams>=3.2,<4; sys_platform == "win32"
winrt-Windows.Globalization>=3.2,<4; sys_platform == "win32"
winrt-Windows.Foundation>=3.2,<4; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=3.2,<4; sys_platform == "win32"
